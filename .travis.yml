language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"

# command to install dependencies
install:
  - travis_retry pip install -r requirements-dev.txt
  - travis_retry python setup.py install

# command to run tests
script:
  - nosetests -v --with-coverage --cover-package=descent

after_success:
  - coveralls
